# CLAUDE.md

The development workflow, project layout, build and test commands, and
contribution rules for this repository live in [AGENTS.md](AGENTS.md).

Read `AGENTS.md` before making any change, and follow it exactly. It is
tool-agnostic and applies to Claude Code the same as to any other coding agent.
Do not duplicate its rules here — update `AGENTS.md` instead so every agent sees
the same instructions.
